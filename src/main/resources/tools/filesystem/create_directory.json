{
  "name": "create_directory",
  "description": "Creates a new directory, including any necessary but nonexistent parent directories.",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "The path of the directory to create.",
        "minLength": 1
      }
    },
    "required": [
      "path"
    ]
  }
}