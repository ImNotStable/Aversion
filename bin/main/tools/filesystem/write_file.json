{
  "name": "write_file",
  "description": "Writes content to a file. Creates or overwrites the file.",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "The path to the file.",
        "minLength": 1
      },
      "content": {
        "type": "string",
        "description": "The content to write to the file.",
        "minLength": 0
      }
    },
    "required": [
      "path",
      "content"
    ]
  }
}